# Home-Windows proof chunks

Base64 slices of `home-windows-pr4-prove.mp4` and `home-windows-pr4-prove.png`.
The `assemble-windows-proof` workflow rebuilds the binaries and publishes GitHub Release `prove-windows-pr4-home`.
